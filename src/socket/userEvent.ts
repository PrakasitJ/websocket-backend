export enum UserListenerEvent {
    USER_CONNECTED = "connect",
    USER_DISCONNECTED = "disconnect",
}

export enum UserEmitterEvent {
    USER_CONNECTED = "connected",
    USER_DISCONNECTED = "disconnected",
}