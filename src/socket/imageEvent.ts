export enum ImageListenerEvent {
    GET_IMAGE_REQUEST = "get_image_request",
    UPLOAD_IMAGE = "upload_image",
}

export enum ImageEmitterEvent {
    GET_IMAGE_RESPONSE = "get_image_response",
    UPLOAD_IMAGE_RESPONSE = "image_uploaded"
}